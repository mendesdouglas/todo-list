spring:
  datasource:
    url: jdbc:mysql://localhost:3307/tododb
    username: user
    password: userpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true